[project]
name = "system-sculptor"
version = "0.1.0"
description = "A tiny pattern recommendation engine for micro‑service design."
authors = [{name = "Axentx"}]
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
